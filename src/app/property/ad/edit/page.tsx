import { EditAd } from "@screens/property/ad/edit";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Hounter | Edit Ad",
};

export default function EditAdPage() {
  return <EditAd />;
}
